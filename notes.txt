python3 -m venv .venv

== activeren ==
source .venv/bin/activate

== deactiveren ==
deactivate

== installer Django ==
python -m pip install Django

== upgrade ==
 pip install --upgrade pip

== herstellen ==
python -m pip install Pillow

== project maken ==
django-admin startproject core . of my project

django-admin startproject myproject

== Django import ==
import Django
print(django.get_version())

== verlaten ==
quit()

== app maken "myproject"==
python3 manage.py startapp “bijvoorbeeld post”

== create superuser ==
python3 manage.py createsuperuser

== bestandsmap ==
cd myproject
 

== run server ==

python3 manage.py runserver



App Starten=====================

python3 manage.py startapp “appnaam”

Views.py inrichten
Urls.py inrichten
 ==== pip ===
pipenv shell

DATABASE

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'django',
        'USER': 'root',
        'PASSWORD': 'root12345',
        'HOST': '127.0.0.1',
        'PORT': '3306',
            'OPTIONS': {'init_command':"SET sql_mode='STRICT_TRANS_TABLES'"}    
             }
}

/usr/bin/ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"
/usr/bin/ruby -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': BASE_DIR / 'db.sqlite3',
    }
}
  <a href="{% url 'posts:list' %}">
                <span role="img" aria-label="Posts" title="Posts">📰</span>
                </a> |
                  <a href="/members">
                   <span role="img" aria-label="Members" title="Members">📄</span>
                    </a> |
                <a href="{% url 'users:register' %}">
                <span role="img" aria-label="User Registratioln" title="Registration">🚀</span>
                </a> |
                <a href="{% url 'users:login' %}">
                    <span role="img" aria-label="User Login" title="Login">🔐</span>
                    </a>

Django CMS


pip install djangocms-versioning
pip install djangocms-alias
pip install django-filer\>=3.0
pip install djangocms-text-ckeditor
pip install djangocms-frontend
pip install djangocms-text
pip install djangocms-file djangocms-picture djangocms-video djangocms-googlemap djangocms-snippet djangocms-style

<section id="about">
            {% placeholder "about" or %}There is no content.{% endplaceholder %}
            </section>


 {% show_menu 0 100 100 100 %}

 """
 DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql', 
        'NAME': 'br118517_zoekhonden',
        'USER': 'br118517_zoekhonden',
        'PASSWORD': 'zoekhond032025',
        'HOST': 'localhost',   
        'PORT': '3306',
    }
}
"""

source /home/br118517/virtualenv/zoekhonden/3.12/bin/activate && cd /home/br118517/zoekhonden

source /home/br118517/virtualenv/roelandstrijk/3.12/bin/activate && cd /home/br118517/roelandstrijk

DATABASES = {

    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': BASE_DIR / 'db.sqlite3',
    }
}

touch django.log

python manage.py collectstatic

Django cms starten=======
djangocms  startproject mysite  --template cms.new.tar.gz

Git zoekhonden token:

